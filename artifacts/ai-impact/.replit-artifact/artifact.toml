kind = "web"
previewPath = "/"
title = "AI Impact Tracker"
version = "1.0.0"
id = "artifacts/ai-impact"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20631

[services.development]
run = "pnpm --filter @workspace/ai-impact run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ai-impact", "run", "build" ]
publicDir = "artifacts/ai-impact/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20631"
BASE_PATH = "/"
